```js
    tableAllow: propTypes.string.def(''), // 表格权限
    searchShow: propTypes.bool.def(true), // 是否显示搜索
    isPage: propTypes.bool.def(true), // 是否分页
    tableSearch: propTypes.bool.def(true), // 搜索刷新
    tableRefresh: propTypes.bool.def(true), // 列表刷新
    defaultGet: propTypes.bool.def(true), // 是否默认执行
    getTableUrl: propTypes.func, // 获取数据api
    tableParams: propTypes.object, // 搜索参数
    opeBtnNames: propTypes.array, // 按钮权限
}]
```
